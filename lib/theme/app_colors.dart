import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF8B30A8); // Morado medio — Header/Nav
  static const secondary = Color(0xFFCC80DD); // Morado claro — Botón secundario
  static const accent = Color(0xFFAA45C0); // Violeta — Botón primario
  static const card = Color(0xFFF0D8F8); // Lavanda muy suave — Fondos tarjetas
  static const textDark = Color(0xFF6B1888); // Morado — Textos
  static const background = Color(0xFF3F1D4B);
  static const xp = Color(0xFFFFD700);
  static const white = Color(0xFFFFFFFF);
}
